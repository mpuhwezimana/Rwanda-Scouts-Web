<!DOCTYPE html>
<html>
<head>
  <title>Posts</title>
</head>
<body>

  <div class="container">
    <div class="row">
      <% @posts.each_slice(3) do |three_posts| %>
          <div class="row">
          <% three_posts.each do |post| %>
            <div class="col-md-4">
              <h2><%= link_to post.title, "/posts/#{post.id}" %></h2>
              <p><%= post.body %></p>
            </div>
          <% end %>
          </div>
      <% end %>
    </div>
  </div>>

</body>
</html>